<div class="hamburger">
  <img class=".hamburger" src="/menu.svg" />
  <div class="nav-links">
    <a class="nav-links" href="#openModal-about">About</a>
    <a class="nav-links" href="#testm">Testimonials</a>
    <a class="nav-links" href="#contact">Contact</a>
  </div>
</div>

<style>
  .hamburger {
    padding-right: 20px;
    width: 3.5rem;
    fill: var(--accent);
  }

  .nav-links {
    border-radius: 0.5rem;
    background-color: white;
    display: none;
    margin: 0;
  }

  .nav-links a {
    display: block;
    text-align: left;
    padding: 0.25rem 1rem;
    text-decoration: none;
    font-weight: 600;
    color: var(--accent);
  }

  .nav-links a:hover,
  a:focus {
    background-color: var(--accent);
  }

  .expanded {
    position: fixed;
    min-width: 29%;
    float:right;
    z-index: 1000;
    display: flex;
    flex-direction: column;
    right:0%;
    top: 5rem;
  }

  @media screen and (min-width: 700px) {
    .nav-links {
      margin-left: 5em;
      display: block;
      position: static;
      width: auto;
      background: none;
    }

    .nav-links a {
      display: inline-block;
      padding: 15px 20px;
    }

    .hamburger {
      display: none;
    }
  }
</style>

<script></script>
